# Çalışma zamanı bağımlılıkları — Python 3.10+
# Kesin sürümler ortam kilitlenince (lock) sabitlenecek.

# Çekirdek
numpy>=1.24
opencv-python>=4.8
Pillow>=10.0
pyyaml>=6.0
tqdm>=4.66
python-dotenv>=1.0

# Tespit / eğitim
ultralytics>=8.3.0
torch>=2.2
torchvision>=0.17

# Değerlendirme (mAP@0.5)
pycocotools>=2.0.7
faster-coco-eval>=1.5

# Dilimleme (SAHI — eğitim/çıkarım tiling)
sahi>=0.11.18
