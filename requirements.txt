# Yerel ortam — veri katmanı ve değerlendirme işleri (hafif, torch YOK).
# Eğitim/çıkarım bağımlılıkları için: requirements-train.txt (Colab).
# Python 3.10+

# Çekirdek
numpy>=1.24
opencv-python>=4.8
Pillow>=10.0
pyyaml>=6.0
tqdm>=4.66
python-dotenv>=1.0

# Değerlendirme (mAP@0.5)
pycocotools>=2.0.7
faster-coco-eval>=1.5
