# Örnek — bu dosyayı configs/data.yaml olarak kopyalayıp `path` değerini
# kendi verinin kök dizinine ayarla. configs/data.yaml git'e girmez.
path: /mutlak/yol/data/uav_ldz
train: train/images
val: val/images
test: test/images

# Sınıf sırası (sabit)
names:
  0: vehicle
  1: human
  2: uap
  3: uai
